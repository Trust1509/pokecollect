fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
pydantic==2.8.2
pydantic-settings==2.3.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 ist mit bcrypt>=4.1 inkompatibel (Login/Passwortwechsel → 500);
# ohne Pin zog pip bcrypt 5.x. Beim Wechsel auf bcrypt 5 auch passlib ersetzen.
bcrypt==4.0.1
python-multipart==0.0.9
httpx==0.27.0
# Cardmarket-OAuth (optionaler Preis-Fallback)
requests-oauthlib==1.3.1
apscheduler==3.10.4
Pillow==10.4.0
# Scan-Variante A (lokale OCR). Gemini (Variante B) läuft per REST über httpx.
pytesseract==0.3.13
